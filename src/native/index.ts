import NativeQuic from './NativeQuic';
import NativeStorage from './NativeStorage';
import NativeIdentityProvisioning from './NativeIdentityProvisioning';
import NativeQuicSession from './NativeQuicSession';

export const NativeModules = {
  NativeQuic,
  NativeStorage,
  NativeIdentityProvisioning,
  NativeQuicSession,
  // Add other shims as needed
};

export default NativeModules;
